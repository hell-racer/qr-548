// Файл конфигурации фиксированных реквизитов получателя (школы).
// Здесь программист/администратор меняет данные — остальной код трогать не нужно.
// Это БЮДЖЕТНЫЙ платёж (через казначейство/УФК).
// Формат сверен с рабочим QR, сгенерированным на https://sbqr.ru/:
//   ST00012|Name=...|PersonalAcc=...|BankName=...|BIC=...|CorrespAcc=...
//   |KPP=...|PayeeINN=...|lastName=...|payerAddress=...|Purpose=...|CBC=...|OKTMO=...|Sum=...
window.SCHOOL_PAYMENT = {
  // Код стандарта ГОСТ Р 56042-2014. Менять не нужно — всегда ST00012.
  ST: "ST00012",

  // Номер казначейского счёта (PersonalAcc).
  PersonalAcc: "03224643450000007300",

  // Наименование банка получателя (сокращённое, как в рабочем QR).
  BankName: "ОКЦ № 1 ГУ БАНКА РОССИИ ПО ЦФО//УФК ПО Г. МОСКВЕ Г МОСКВА",

  // БИК банка получателя (9 цифр).
  BIC: "004525988",

  // Корреспондентский счёт банка получателя.
  CorrespAcc: "40102810545370000003",

  // ИНН получателя платежа.
  PayeeINN: "7737027335",

  // КПП получателя платежа.
  KPP: "772401001",

  // КБК (код бюджетной классификации) — тег CBC в QR (в эталоне CBC, а не Categ).
  CBC: "00000000000131131022",

  // ОКТМО — тег OKTMO в QR.
  OKTMO: "0",

  // Наименование получателя (тег Name) — как в рабочем QR.
  Name: "Департамент финансов города Москвы (ГАОУ Школа N 548 \"Царицыно\", л/с 2807552000960482)",

  // Данные плательщика (необязательные). Заполняются из полей формы приложения.
  // ФИО плательщика (тег lastName).
  PayerLastName: "",

  // Адрес плательщика (тег payerAddress).
  PayerAddress: ""
};

// --- Настройки занятий и шаблона назначения платежа ---
window.APP_SETTINGS = {
  // Список занятий по умолчанию (показывается новому пользователю).
  // Формат: массив объектов { name, sum } — название занятия и сумма за него.
  // Каждый родитель может добавить свои занятия — они сохраняются в браузере.
  DefaultLessons: [
    { name: "Создание инженерных систем на базе модульных конструкторов", sum: "2000" },
    { name: "Практикум по читательской грамотности на английском языке", sum: "2000" },
    { name: "Тренинг по решению математических задач", sum: "1200" },
    { name: "Тренинг по русскому языку", sum: "1200" }
  ],

  // Шаблон назначения платежа по умолчанию.
  // Подставляются плейсхолдеры:
  //   <ФИО>          — ФИО ребёнка из данных
  //   <класс>        — класс ребёнка
  //   <занятие>      — название занятия (своё для каждого QR)
  //   <период>       — период оплаты (например, "сентябрь 2026")
  // Пользователь может изменить шаблон — он сохраняется в браузере.
  DefaultTemplate: "ПОУ, <ФИО>, <класс>, <занятие>, <период>"
};
